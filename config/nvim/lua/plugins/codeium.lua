return {
  "Exafunction/codeium.vim",
  -- Intentionally disabled.
  -- Without an authenticated Codeium account, this plugin can spam startup
  -- warnings and hang headless Neovim checks. Keep the marker here so fresh
  -- installs do not silently re-enable it.
  enabled = false,
}
