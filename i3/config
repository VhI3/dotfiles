# Default modkey
set $mod Mod4

# Hide titlebar
new_window pixel

# i3-gaps
gaps inner 8

# Font for window titles.
font pango: SpaceMono Nerd Font Mono Medium 11

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec hyper

# Kill focused window
bindsym $mod+Shift+q kill

# Set monitor arrangement on TinkStation with 3 Monitors
exec --no-startup-id ~/.config/setMonitors/setMonitor.sh

# variables for left and right monitor 
# (Left: VGA-1 Right: HDMI-1)
set $monEven HDMI-1   
set $monOdd VGA-1    


# Set Keyboard layouts
exec_always "setxkbmap -layout us,de,ir"
exec_always "setxkbmap -option 'grp:alt_shift_toggle'"
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Start rofi
bindsym $mod+d exec rofi -show drun -columns 3
#bindsym $mod+d exec ~/.config/rofi/launchers/launcher.sh 
#bindsym $mod+d exec dmenu_run

# Move between windows
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# Alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# Alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Split in horizontal orientation
bindsym $mod+o split h

# Split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Name the workspaces
set $workspace1 "1: " 
set $workspace2 "2: "
set $workspace3 "3:﫯 "
set $workspace4 "4: "
set $workspace5 "5: "
set $workspace6 "6: "
set $workspace7 "7: "
set $workspace8 "8: "
set $workspace9 "9: "
set $workspace0 "10: "
set $workspace11 "11: "
set $workspace12 "12:"
set $workspace13 "13:"
set $workspace14 "14:"
set $workspace15 "15:"
set $workspace16 "16:"
set $workspace17 "17:"
set $workspace18 "18:"
set $workspace19 "19:"
set $workspace20 "20:"
set $workspace21 "21:"
set $workspace22 "22: "

# Switch to workspace
bindsym $mod+1 workspace $workspace1
bindsym $mod+2 workspace $workspace2
bindsym $mod+3 workspace $workspace3
bindsym $mod+4 workspace $workspace4
bindsym $mod+5 workspace $workspace5
bindsym $mod+6 workspace $workspace6
bindsym $mod+7 workspace $workspace7
bindsym $mod+8 workspace $workspace8
bindsym $mod+9 workspace $workspace9
bindsym $mod+0 workspace $workspace0
bindsym $mod+F1 workspace $workspace11
bindsym $mod+F2 workspace $workspace12
bindsym $mod+F3 workspace $workspace13
bindsym $mod+F4 workspace $workspace14
bindsym $mod+F5 workspace $workspace15
bindsym $mod+F6 workspace $workspace16
bindsym $mod+F7 workspace $workspace17
bindsym $mod+F8 workspace $workspace18
bindsym $mod+F9 workspace $workspace19
bindsym $mod+F10 workspace $workspace20
bindsym $mod+F11 workspace $workspace21
bindsym $mod+F12 workspace $workspace22

# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+6 move container to workspace $workspace6
bindsym $mod+Shift+7 move container to workspace $workspace7
bindsym $mod+Shift+8 move container to workspace $workspace8
bindsym $mod+Shift+9 move container to workspace $workspace9
bindsym $mod+Shift+0 move container to workspace $workspace0
bindsym $mod+Shift+F1 move container to workspace $workspace11
bindsym $mod+Shift+F2 move container to workspace $workspace12
bindsym $mod+Shift+F3 move container to workspace $workspace13
bindsym $mod+Shift+F4 move container to workspace $workspace14
bindsym $mod+Shift+F5 move container to workspace $workspace15
bindsym $mod+Shift+F6 move container to workspace $workspace16
bindsym $mod+Shift+F7 move container to workspace $workspace17
bindsym $mod+Shift+F8 move container to workspace $workspace18
bindsym $mod+Shift+F9 move container to workspace $workspace19
bindsym $mod+Shift+F10 move container to workspace $workspace20
bindsym $mod+Shift+F11 move container to workspace $workspace21
bindsym $mod+Shift+F12 move container to workspace $workspace22

# Always open some programs on specified workspace
assign [class="Brave-browser"]    $workspace2
assign [class="Hyper"]    $workspace1
assign [class="Thunderbird"]   $workspace3
for_window [class="Spotify"] move to workspace $workspace0
assign [class="Spotify"] $workspace0
assign [class="MATLAB R2020b"] $workspace8
assign [class="qpdfview"] $workspace9
assign [class="TelegramDesktop"] $workspace7
assign [class="Gnome-terminal"] $workspace1
assign [class="Nemo"] $workspace11
assign [class="Code"] $workspace5
assign [class="Texmaker"] $workspace4
assign [class="jetbrains-clion"] $workspace15
# Enable float for some windows
#for_window [class="^Pavucontrol$"] floating enable
#for_window [class="^Arandr$"] floating enable
#for_window [class="^Steam$" title="^Friends List$"] floating enable
#for_window [class="^Steam$" title="Steam - News"] floating enable
#for_window [class="^Steam$" title=".* - Chat"] floating enable
#for_window [class="^Steam$" title="^Settings$"] floating enable
#for_window [class="^Steam$" title=".* - event started"] floating enable
#for_window [class="^Steam$" title=".* CD key"] floating enable
#for_window [class="^Steam$" title="^Steam - Self Updater$"] floating enable
#for_window [class="^Steam$" title="^Screenshot Uploader$"] floating enable
#for_window [class="^Steam$" title="^Steam Guard - Computer Authorization Required$"] floating enable
#for_window [title="^Steam Keyboard$"] floating enable
for_window [class="Yad"] floating enable

workspace  $workspace1 output $monOdd
workspace  $workspace2 output $monEven
workspace  $workspace3 output $monOdd
workspace  $workspace4 output $monEven
workspace  $workspace5 output $monOdd
workspace  $workspace6 output $monEven
workspace  $workspace7 output $monOdd
workspace  $workspace8 output $monEven
workspace  $workspace9 output $monOdd
workspace  $workspace0 output $monEven
workspace  $workspace11 output $monOdd
workspace  $workspace12 output $monEven
workspace  $workspace13 output $monOdd
workspace  $workspace14 output $monEven
workspace  $workspace15 output $monOdd
workspace  $workspace16 output $monEven
workspace  $workspace17 output $monOdd
workspace  $workspace18 output $monEven
workspace  $workspace19 output $monOdd
workspace  $workspace20 output $monEven
workspace  $workspace21 output $monOdd
workspace  $workspace22 output $monEven

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

bindsym $mod+Shift+e exec rofi -show power-menu -modi power-menu:~/.local/bin/rofi-power-menu   -theme Paper   -font "Hack Nerd Font Mono 16"   -width 20   -lines 6

#Lock the screen 
bindsym $mod+Shift+x exec ~/.config/betterlockscreen/betterlockscreen --lock blur

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Defining colors
set $bg      #282A36
set $lg      #44475a
set $fg      #f8f8f2
set $cyan    #8be9fd
set $green   #50fa7b
set $magenta #ff79c6
set $comment #6272a4
set $purple  #bd93f9

# Set some nice colors      border	 background text     indicator
client.focused              $comment $comment   $fg      $comment
client.unfocused            $bg      $bg        $cyan    $bg
client.focused_inactive     $bg      $bg        $green   $bg
client.urgent               $bg      $bg        $magenta $bg
client.placeholder          $bg      $bg        $cyan    $bg

bar {
    status_command    python3 ~/.config/i3pystatus/i3pystatus.conf
    position          top
    workspace_buttons yes
    font pango:Hack, FontAwesome 10
    colors {
#        background #253941
#        statusline #e7dfd9
        separator  #081419
        focused_workspace  #2aa198 #073642 #eee895
#        active_workspace   #073642 #859900 #839496
        inactive_workspace #002b36  #002b36   #586e75
        urgent_workspace   #cb4b16 #dc322f #fdf6e3
    }
  }

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
#exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id xss-lock --transfer-sleep-lock -- ~/.config/betterlockscreen/betterlockscreen


exec_always --no-startup-id ~/.config/setWallpaper/wallpapers.sh
#exec --no-startup-id xautolock -time 5 -locker ~/.config/matrixlock/matrixlock.py
exec --no-startup-id nm-applet
exec --no-startup-id volumeicon
exec --no-startup-id sciebo
exec --no-startup-id clipit
exec_always "setxkbmap -option caps:swapescape"
